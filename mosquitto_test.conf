# === Listener Setup ===
listener 1883
allow_anonymous true           # Allow connection without authentication

# === Payload Size Limit ===
max_packet_size 5242880        # Set maximum allowed packet to 5 MB

# === Optional Logging (for debugging) ===
log_type all
connection_messages true